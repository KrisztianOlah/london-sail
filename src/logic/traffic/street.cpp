#include "street.h"

Street::Street(const QString& c, const QString& d, const QString& n) : closure(c),
                                                                       directions(d),
                                                                       name(n)
{
}
